'use client'

/**
 * ListingHub theme header - wraps theme navbar.
 * Use with ThemeLayout for public-facing theme pages.
 */
import NavbarDark from '@/components/theme/navbar/navbar-dark'

export default function ThemeHeader() {
  return <NavbarDark />
}
